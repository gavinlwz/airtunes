<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#ff000000"
    tools:context="com.mycompany.airtunes.MainActivity">

    <TextView
        android:id="@+id/groupTagView"
        android:text="Search for a group: "
        android:textColor="#ffffffff"
        android:layout_alignParentTop="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <SearchView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/searchGroup"
        android:layout_below="@id/groupTagView"
        android:background="#87cefa"
        android:minWidth="100dp"
        android:layout_alignParentLeft="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/searchGroup"
        android:id="@+id/enterGroup"
        android:onClick="onAddSongButtonClick"/>

    <TextView
        android:id="@+id/userTagView"
        android:text="Search for a user:"
        android:textColor="#ffffffff"
        android:layout_below="@id/enterGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <SearchView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/userSearchView"
        android:layout_below="@id/userTagView"
        android:background="#87cefa"
        android:minWidth="100dp"
        android:layout_alignParentLeft="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/userSearchView"
        android:id="@+id/goToUserProfile"
        android:onClick="onAddSongButtonClick"/>



</RelativeLayout>
